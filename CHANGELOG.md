# CineScope Studio 更新日志

本文件记录面向用户的重要变化。版本日期使用北京时间。

## [0.2.0] - 2026-09-01

### 版本概览

`v0.2.0` 是一次完整的产品化重构：项目从“豆瓣数据抓取 + 推荐报告”升级为本地运行的影视同步、推荐、资料核验和可视化探索工作台。核心目标是让候选范围更广、推荐理由更可信、媒体加载更稳定，并让用户可以用自然语言或多个作品主动控制推荐方向。

### 全新观影雷达与多焦点融合

- 新增“观影雷达”页面，把最近观看、在线候选和口味关系组织成可缩放、可拖动的关系图。
- 新增“焦点配方台”：可搜索电影、剧集或动画并手动指定第一个焦点。
- 第一次手动选择会替换系统自动起点，避免自动种子干扰用户明确意图。
- 可继续搜索或直接点击关系图节点，最多同时加入三部作品。
- 单焦点生成相似推荐；双焦点或三焦点进入交集 / 混合推荐。
- 三个焦点已满后继续添加会替换最早焦点，减少额外管理步骤。
- 搜索采用 `420ms` 防抖，可取消旧请求；回车默认选择排序第一的同名候选。
- 同名候选显示媒介类型、年份和评分，降低电影版、剧集版、动画版选错概率。
- 重组逻辑综合类型、气质、主创、地区、语义、口碑、焦点覆盖率和同质惩罚。
- 展示本轮配方、候选规模、严格交集数量、评分覆盖率和各信号权重。
- 节点标题重新布局，移除文字前装饰竖杠，单行 / 双行 / 状态文字均水平和垂直居中。
- 优化节点避让、轨道分层、连线标签、缩放中心、滚轮翻页和平移手势。
- 鼠标指向节点时使用紧凑速读卡，不再一次堆叠大量难以阅读的小字。

### CineScope V3 视觉与交互重构

- 统一深夜蓝黑、暖金、青色和紫色的产品视觉系统。
- 重做左侧导航的图标、字号、激活态与可读性。
- 首页改为紧凑但有层级的内容组织，减少彼此割裂的大块空白。
- 重做主轮播：保证画面比例、标题和操作区对齐，并修复编号按钮无法切换指定作品的问题。
- “今晚”新增自然语言入口，可直接描述题材、节奏、气氛、相似作品和避雷条件。
- 电影、电视剧、动画剧集保持三个独立频道，分别维护批次、上一批和耗尽恢复状态。
- 片库卡片重新平衡海报、简介和剧照尺寸，修复标题下半部被裁切、海报被剧照遮挡和卡片高低不齐。
- 横向剧照使用真实比例容器、懒加载和预热；离开详情再返回时保留横向与纵向浏览位置。
- 不具备可用剧照的卡片不再渲染空黑框或假占位。
- 详情页改为更紧凑的分区式布局，减少长文本造成的大面积留白。
- 详情页新增清晰的返回入口，并统一“查看详情”等操作按钮的尺寸和基线。
- 评分证据、观影记录和已核资料在信息缺失时使用稳定网格，不再出现模块一高一低。
- 演员与导演卡片支持人物图片状态、缺失兜底和后台修复。
- 在线搜索入口、候选来源和本地 / 在线状态改为更明确的用户可见文案。
- 繁体中文和英文标题增加简体中文本地化与中文优先展示策略。

### 推荐算法与候选质量

- 新增跨来源候选发现层，区分本地档案、精选候选、在线扩展和实时新片来源。
- 新增目录水合流程，在候选进入排序前补齐中文标题、年份、媒介类型、评分、类型、主创和简介。
- 新增语义信号模块，支持自然语言意图、相似作品和多焦点组合特征。
- 新增评分归一化与样本覆盖统计，避免不同来源的评分口径直接混用。
- 加强作品身份归一化，合并同一作品的中文名、英文名、别名、不同来源 ID 和不同图片版本。
- 修复“推荐同类型”重复出现同一作品、同一作品多条记录以及科学纪录片 / 电视剧类型混淆。
- 对推荐池增加同质惩罚和多样性重排，减少反复出现相同少数影片。
- 本地已看、永久排除和当前会话反馈会在新批次中继续生效。
- 推荐理由从模板化文案升级为基于共同类型、主创、地区、气质、近期口味和口碑证据的动态说明。
- 不展开卡片也能看到核心推荐依据，展开后再展示完整证据和简介。
- 保留本地确定性排序为事实源；模型只用于可选的意图结构化和说明生成。

### 在线发现与中文资料

- 新增全局发现服务，扩展电影、电视剧和动画剧集候选。
- TVMaze、AniList、Jikan / MyAnimeList 可作为免 Key 的剧集与动画补充来源。
- TMDb、OMDb / IMDb 继续作为可选的元数据和媒体来源。
- 在线资料优先映射为简体中文标题；无法可靠翻译时保留可核验的原始名称，而不是生成错误译名。
- 简介采用中文优先策略，并避免将明显不完整的英文片段直接占据主要展示区域。
- 在线候选与本地片库分层显示，避免用户误以为推荐范围只取决于本机记录数量。

### 媒体加载、缓存与失败恢复

- 建立内容哈希媒体仓库，验证通过的海报、背景、剧照和人物图按 SHA-256 保存。
- 新版前端优先消费本地 `/media/<hash>`，不把未经验证的外链直接交付浏览器。
- 图片验证覆盖 MIME、文件魔数、像素解码、尺寸以及作品 / 人物身份匹配。
- 新增媒体来源编排、URL 候选、失败回退和后台修复任务。
- 图片组件支持懒加载、邻近预热、请求去重、失败状态和设计兜底。
- 横向剧照在滚动接近可视区域时提前加载，降低拖动后长时间空白的概率。
- 详情请求和图片请求分开缓存，减少重复打开同一作品时的等待时间。
- 无图片时使用统一设计占位，不再显示浏览器默认的破图图标。
- 媒体健康接口与页面可显示 ready、missing、designed-fallback 等状态。

### 豆瓣同步与自动化

- 同步任务默认自动抓取到末页，`250` 页只作为异常循环的安全阀。
- 支持部分分页失败后的续跑，保留已经成功抓取的条目作为恢复种子。
- 诊断可区分真实空页、需要登录、隐私权限、安全验证和解析失败。
- 支持保存用户 ID 与自动同步设置，并按计划执行增量检查。
- 从豆瓣返回 CineScope 或重新显示标签页时可触发带节流和并发去重的增量同步。
- 同步完成后会刷新本地片库，并立即从推荐会话中移除最新已看作品。
- 修复“未分页”“0/256”“需要登录态但缺少明确处理入口”等状态表达。
- 启动脚本会停止旧 CineScope 实例、等待端口释放、记录真实 PID 并后台启动新服务。

### Cookie 与隐私边界

- Cookie 只能由用户在可见输入框中手动提供。
- Cookie 仅存在于当前标签页 `sessionStorage` 和本次本机请求内。
- 关闭标签页后会话 Cookie 失效；数据库、缓存、日志、任务响应和报告均不保存 Cookie。
- 不读取浏览器 Profile、请求头、剪贴板、密码、历史记录或隐藏存储。
- 不接受整段请求头、多行说明文字或带字段名前缀的内容作为 Cookie。
- 主页 URL 仅用于解析豆瓣用户 ID，不能替代登录 Cookie。
- 代理仅接受本机 HTTP / SOCKS 端口，不接收订阅地址。
- 可选服务 API Key 通过环境变量配置，不写入 URL，也不在 API 响应中回显。

### API 与本地状态

- 推荐、反馈、Catalog、同步和媒体接口统一采用 `schema_version: 2`。
- 推荐会话可以完整恢复电影、电视剧和动画剧集三个频道的独立游标。
- 反馈采用 append-only 事件流，撤销会追加 undo 事件，不会静默改写历史。
- 新增或扩展：
  - `POST /api/v2/recommend/sessions`
  - `GET /api/v2/recommend/sessions/<session-id>`
  - `POST /api/v2/recommend/sessions/<session-id>/batch`
  - `POST /api/v2/recommend/sessions/<session-id>/previous`
  - `POST /api/v2/feedback`
  - `POST /api/v2/feedback/<event-id>/undo`
  - `GET /api/v2/library`
  - `GET /api/v2/taste`
  - `GET /api/v2/titles/<title-id>`
  - `GET /api/v2/people/<person-id>`
  - `GET /api/v2/observatory`
  - `POST /api/v2/sync/jobs`
  - `GET /api/v2/media/health`
- 新增路由级滚动恢复、片库横向位置恢复和详情返回来源恢复。

### 性能与稳定性

- 搜索输入、媒体加载和自动同步均增加防抖、节流或并发去重。
- 搜索请求可取消，旧请求结果不能覆盖较新的输入状态。
- 页面只预热当前视口与邻近条目的详情和媒体，避免一次性请求整个片库。
- 卡片骨架、设计兜底和媒体完成态使用一致尺寸，降低加载中的布局跳动。
- 路由切换保留稳定页面状态，返回片库不再重新回到横向剧照最左端。
- 服务启动与端口占用检测增加超时、真实 PID 校验和非 CineScope 进程保护。

### 文档与项目主页

- 重写 GitHub README 的首屏信息架构、快速开始、页面地图、架构说明和隐私边界。
- 新增项目原生 SVG Hero、工作流图和三张真实运行界面截图。
- 新增本更新日志，明确用户可见变化、隐私模型、升级方式和已知限制。

### 测试与验收

- 全量自动化测试：`1082 passed`。
- 契约子测试：`581 subtests passed`。
- 覆盖推荐、Catalog、媒体、同步、评分、本地化、语义、观影雷达、浏览器授权边界和统一 UI 契约。
- 观影雷达已通过真实 Edge / Playwright 验收：首焦点替换、第二焦点融合、文字居中、无窄竖条绘制、无控制台错误、无页面异常和无请求失败。

### 升级方式

```powershell
git pull
python -m pip install -e .
.\run_app.ps1
```

现有本地数据默认继续保存在 `%LOCALAPPDATA%\CineScope`；也可以使用 `CINESCOPE_DATA_DIR` 指定独立目录。

### 已知限制

- 豆瓣分页能否公开访问取决于账号隐私和豆瓣当前策略；需要登录态时仍需用户手动提供当前 Cookie。
- 在线来源的可用性、地区语言和图片完整度会随第三方服务变化。
- TMDb 和 OMDb 的完整能力需要用户自行配置对应 API Key；TVMaze、AniList 和 Jikan 可免 Key 使用。
- 语言模型不是必需依赖；未配置 endpoint 或请求失败时会回退本地规则。
- Windows 提供完整的一键启动脚本；其他系统使用 Python 模块方式启动。

## [0.1.0] - 初始版本

- 豆瓣看过 / 想看抓取。
- CSV 输入与命令行 HTML 推荐报告。
- 基础口味画像、候选排序和本地 Web 界面。
